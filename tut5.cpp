#include<iostream>
using namespace std;
int main()
{
    int num1 , num2;
   // cout<< it is useing to intertion operator(output ko screen per dedo)
  // cin>> is called EXTRACTION operator(user se value lene k liye)
   cout<< "Enter te value of num1 and num2:\n";
   cin>> num1 >> num2;
   cout<< "the value of num1 and num2 is=>"<<num1+num2;
return 0;
}